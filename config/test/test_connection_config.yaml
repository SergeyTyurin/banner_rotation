app:
  host: 127.0.0.1
  port: 8081
database:
  host: 127.0.0.1
  port: 5432
  name: "rotation"
  url: host={host} port={port} user={user} password={password} dbname={dbname}
message_broker:
  host: 127.0.0.1
  port: 5672
  url: amqp://{user}:{password}@{host}:{port}/

